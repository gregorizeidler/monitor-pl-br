# Workflow do GitHub Actions para coletar Votações e Medidas Provisórias

name: Monitor PL - Coletor Completo

on:
  # EXECUÇÃO AUTOMÁTICA DESABILITADA
  # schedule:
    # Roda 2x por dia: 9h e 17h (Brasília) = 12h e 20h (UTC)
    # - cron: '0 12,20 * * *'
  
  # Permite execução manual
  workflow_dispatch:

jobs:
  coletar_dados:
    runs-on: ubuntu-latest
    
    steps:
      - name: 1. Checkout do repositório
        uses: actions/checkout@v3
      
      - name: 2. Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 3. Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 4. Executar coletor completo
        run: python3 coletar_todos_dados.py
      
      - name: 5. Commit e Push das mudanças
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add estado.json
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 Atualiza dados: Votações e MPs [$(date +'%Y-%m-%d %H:%M')]"
          git push

